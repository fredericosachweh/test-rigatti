services:
  mongo:
    image: mongo:7
    platform: linux/arm64
    container_name: modena-spo-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: modena_spo
    volumes:
      - modena_mongo_data:/data/db

volumes:
  modena_mongo_data:
